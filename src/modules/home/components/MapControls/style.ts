import styled from '@emotion/styled';

export const MapControlsContainer = styled.div`
  position: absolute;
  height: 100%;
  top: 1rem;
  right: 1rem;
  z-index: 9;
  display: flex;
  flex-direction: column;
  gap: 0.5rem;
`;
