import { ModalLoadTiff, MapControls, MapContainer } from '../../components';

const Home = () => {
  return (
    <>
      <MapContainer />
      <MapControls />
      <ModalLoadTiff />
    </>
  );
};

export default Home;
