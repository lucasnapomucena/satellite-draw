import { extendTheme } from '@chakra-ui/react'

export const config = {
    colors : {
        brand: {
            900: '#1a5d35',
            800: '#167b51',
            700: '#2aac5c',
          },
    }
}

export const theme = extendTheme({ config });
